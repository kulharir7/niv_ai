# Core LLM
openai>=1.0.0
tiktoken>=0.7.0

# LangChain Stack
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langgraph>=0.3.0

# Optional: Anthropic/Google providers (install manually if needed)
# langchain-anthropic>=1.0.0
# langchain-google-genai>=2.0.0

# MCP (Model Context Protocol) - for remote MCP servers only
# Same-server (FAC) uses direct Python calls, no SDK needed
langchain-mcp-adapters>=0.1.0

# Vector Store (for RAG/knowledge base)
faiss-cpu>=1.7.0

# Document Processing
pdfplumber>=0.10.0
python-docx>=1.0.0
Pillow>=10.0.0

# Data Processing
pandas>=2.0.0
openpyxl>=3.1.0
numpy>=1.24.0

# TTS (Voice mode)
edge-tts>=6.1.0

# Utils
pydantic>=2.0.0
httpx>=0.27.0
