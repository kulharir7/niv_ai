openai
tiktoken
pytesseract
pdfplumber
python-docx
Pillow
pandas
openpyxl
razorpay
# piper-tts is optional — install manually if TTS is needed:
#   pip install piper-tts
# It may not work on all platforms (requires Python 3.9+ and specific OS support)
