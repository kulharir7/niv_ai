openai
tiktoken
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.3.0
langchain-community>=0.3.0
faiss-cpu
pytesseract
pdfplumber
python-docx
Pillow
pandas
openpyxl
razorpay
# MCP SDK is OPTIONAL — only needed for REMOTE MCP servers (non-localhost).
# Same-server (FAC on localhost) uses direct Python import, zero deps needed.
# Install manually if connecting to remote MCP servers:
#   pip install mcp langchain-mcp-adapters
edge-tts
# piper-tts is optional — install manually for OFFLINE TTS fallback:
#   pip install piper-tts
# It may not work on all platforms (requires Python 3.9+ and specific OS support)
